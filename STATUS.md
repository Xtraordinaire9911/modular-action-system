# Implementation Status

This page separates implemented behavior from partial integrations and future
research claims. The authoritative integration branch is `develop`; feature
branches must pass CI before being merged there, and `main` changes only through
a verified release pull request.

| Claim | Status | Code evidence | Test or demo evidence |
|---|---|---|---|
| Canonical runtime state | Implemented | `src/runtime/cognitive_map.py` is the only mutable episode map; `src/planner/cognitive_map.py` derives a read-only planner view. | `tests/test_architecture_unification.py` |
| Multi-source fusion gate | Implemented, calibrated rule-first | `src/verification/conflict_detector.py` retains raw assertions, handles conflicting and required-but-missing sources, writes accepted fused state, and blocks System 1 on unresolved uncertainty. | `tests/test_epistemic_runtime.py`, `tests/test_fusion_calibration.py`; live conflict/calibration cases |
| Active perception | Runtime interface implemented; a real probe now sits behind it | `src/verification/active_perception.py` accepts an injected perception probe and re-observes before continue/escalate. Runtime contains no VLM invocation policy. `src/perception/vlm_perception_probe.py` is a probe that photographs the disputed region during the probe and returns the model's answer as a `source="visual"` assertion; it returns `None` rather than an empty observation when it cannot contribute, so the resolver escalates instead of recording a look that found nothing. | live `dom_wot_conflict_active_perception`; `tests/test_vlm_perception_probe.py`, including one test through the real `ActivePerceptionResolver` |
| Which conflicts reach active perception | **Measured, and narrower than it looks** | A DOM-versus-screen disagreement does **not** block with the default arbiter, so active perception is never invoked for it: a boolean disagreement scores 1.0, multiplied by the more reliable of the two sources (`dom` at 0.8), against a halt threshold of 1.0 — measured at 0.8 and filtered out. The demo's false success is therefore caught by the episode's own verification comparing the two answers, **not** by the probe. A device-versus-screen conflict does reach the resolver, because `wot` is weighted 1.0. | `tests/test_vlm_perception_probe.py` pins the threshold, both reliabilities and the resulting mass, so this claim fails loudly if the defaults change |
| Bounded zero-shot action planning | Implemented | `GoalSpec -> ActionContext -> DeclarativeRuntimeTaskPlanner -> PrimitiveAction`; no benchmark keyword/regex plan is hidden in the runtime. | `tests/test_runtime_goal_episode.py`; live normal-goal case |
| Natural-language intent to `GoalSpec` handoff | Implemented for bounded supported intents; model optional | `scripts/run_intent_episode.py` passes the `GoalSpec` produced by `IntentPlanner` through `RuntimeEpisodeSpec.goal_spec` into `RuntimeEpisodeRunner.run_goal_episode` and CIM. The labelled rule fallback is used when no model client is configured; unrestricted intent inference is not claimed. | `tests/test_intent_planner.py`, `tests/test_runtime_goal_episode.py`; `scripts/run_intent_episode.py` |
| Vision model as a second source of evidence | Implemented, measured; not a trained detector | `src/perception/vlm_observer.py` sends a crop of the verification region to a vision model and returns a judgement with provenance, confidence and the screenshot digest; an unusable answer contributes nothing rather than being assumed. Spend is capped per run and identical pixels are never billed twice. | `tests/test_vlm_observer.py`; `scripts/eval_model_value.py` (detection 100% over 11 DOM-wrong trials, false alarm 0% over 8, stability 1.00, 1 transport error reported rather than dropped); `scripts/run_llm_demo.py` |
| Model demo runs in the declared use case | Implemented, both surfaces exercised | `scripts/run_llm_demo.py` runs four scenes on the smart room, not on a shopping mock. Three act on the dashboard through its own controls; one has no control available and is completed by resolving a writable property from the Thing Descriptions discovered at `:8082`, writing over WoT, and waiting for the device to report having reached it. The room is reset first, so a repeated run cannot confirm a value the previous run left behind. | `tests/test_llm_demo.py` (asserts the scenes touch both surfaces and that no goal is claimed by both); `artifacts/llm_demo/run-report.json` carries the surface per scene; recorded run at `artifacts/llm_demo/llm-vs-rules-smartroom.mp4` |
| Commanded state and measured state are distinct | Implemented in the servient and consumed by the agent | `env/node_wot_server/server.js` advances measured values on a tick instead of assigning them from the command. A setpoint is accepted immediately; `currentTemperature`, `blinds.measuredPosition` and `projector.lamp` arrive later or not at all. `lamp_failure` and `motor_jam` produce a 2xx write, a setpoint that reads back correctly, and a room that never complies — a failure shape a purely digital surface cannot produce. The agent waits for the measurement with a deadline and reports how long it took. | `tests/test_smartroom_physics.py` against the running servient; `/state` reports the time scale and per-device rates |
| Physical realism of the device layer | **Simulated, and shallow** | The servient models *timing and compliance* — lag, warm-up, travel, and two ways an accepted command fails to happen. It models no thermodynamics, no ambient coupling, no sensor noise or drift, no device-to-device physical interaction, and no hardware. It runs at 30× real time so a demo fits a meeting, which `/state` reports. It is a protocol-faithful and timing-faithful stand-in for a device layer, **not evidence about physical systems**, and no measurement taken here transfers to real hardware. | `env/node_wot_server/server.js`; `/state` `physics` block |
| Composite device goal verified per property | Implemented, verified against the running servient | `src/planner/device_binding.py` declares `room_prepared` as four single-device goals; `src/runtime/device_goal.py` writes each property and reads it back, and reports the goal as met only where the observed value equals the requested one. Optional parts the room does not publish are reported as skipped; required ones fail the goal. Nothing in the bindings names an endpoint - targets come from the TDs discovered at runtime. | `tests/test_device_goal.py`, `tests/test_device_binding.py`; `pytest -m smartroom` (`tests/test_smartroom_room_prepared.py`) against `env/docker-compose.yml`; `artifacts/room_prepared/room_prepared_report.json` |
| Observe-plan-act-verify loop | Implemented | CIM executes one primitive at a time, re-observes, re-fuses, replans, and enforces episode budgets. | `tests/test_runtime_goal_episode.py`; live normal-goal case |
| Recovery decision and execution | Runtime side implemented; a model-driven planner now available behind the port | Runtime returns non-transparent failures as `FailureContext` through `PlannerPort`, validates the returned primitive against fresh affordances, executes, re-observes, verifies, and resumes. The default controller deliberately escalates instead of implementing someone else's recovery policy. `src/planner/model_recovery_planner.py` is an implementation that asks a model which observed affordance recovers the failure, offering only affordance ids and the recovery relations the environment itself declared — no selector or endpoint reaches the prompt. It refuses an invented id, an action outside the primitive action type, an action the episode disallows, and an unusable reply; with no client configured it escalates exactly as the runtime does today. | `YIXIN_RUNTIME_RECOVERY_DOSSIER.md`, `tests/test_agent_runtime_replan_contract.py`, `tests/test_precondition_repair.py`, `tests/test_model_recovery_planner.py` (two tests drive the real `ContinuousInteractionManager`) |
| False-success detection | Implemented | Transition records separate executor success from postcondition success. | live postcondition-mismatch case |
| System-1 reflex cache | Implemented | Cached grounding is used only after fusion/safety/precondition gates and is invalidated after failure; the live repeat case reports warm-up/repeat episode ids, cache-hit rate, routing latency, total transition latency, and amortized latency. | `tests/test_epistemic_runtime.py`, `tests/test_live_runtime_demo.py`; live repeat case |
| Transition/failure ledger | Implemented | Append-only transition JSONL links episode, state ids, affordance key, backend, verification, and recovery. | `src/runtime/episode.py`, `src/adaptation/trace_ledger.py` |
| Trace-driven policy/skill evolution | Implemented as proposals only | Repeated verified evidence can create candidate proposals; production policy or skill semantics are never auto-modified. | `tests/test_skill_proposal.py`, `tests/test_policy_closed_loop.py` |
| Live runtime metrics | Implemented | Metrics are derived from runtime results and transition ledgers and carry `data_source` plus episode ids. | live `measured_metrics.json`; `tests/test_metrics_aggregator.py` |
| Unified episode runner | Implemented | `RuntimeEpisodeRunner` is the shared runtime envelope for structured `GoalSpec` episodes and scripted benchmark/demo envelopes; scripted solvers are labeled separately and are not claimed as agentic planning. | `tests/test_runtime_episode_runner.py`, `tests/test_miniwob_tasks.py`, `tests/test_pipeline.py` |
| Repeated fusion/recovery campaign | Implemented, initial full campaign complete | The campaign runner creates deterministic 7-condition × N repetition plans, records unique episode ids/seeds, reset evidence, independent oracle labels, replay config, and condition-level precision/recall/false-halt/miss/latency summaries. An initial 30×7 live campaign has been run and saved as evidence; broader publication claims still require independent reruns/holdout discipline. | `tests/test_live_fusion_campaign.py`; `artifacts/live_fusion_campaign_full/fusion_campaign_summary.json` |
| Browser isolation | Implemented as Playwright context isolation | Each task gets an isolated browser context. This is a web isolation analogue, not a claim of full Windows UFO2 PiP/RDP isolation. | `src/perception/browser_session.py` |
| Project PiP MVP | Implemented for serialized web/WoT episodes | The isolated entry points atomically lease/checkpoint/reset WoT, recreate the browser context before observation, restore in `finally`, and exclude runtime overlays. A server-held lease protects the single global mock room across separate providers. | `src/isolation/episode.py`; `tests/test_episode_isolation.py`, `tests/test_runtime_intervention.py` |
| A second episode inherits nothing | Verified against the running servient and a real browser | Two claims, checked separately: what an episode *starts from* — a device written to 27 and a key left in `localStorage` by the first episode are both absent from the second, which also carries a distinct episode id — and what an episode *leaves behind*, since dispose restores the state captured at provision rather than the state the episode ended in. Both writes in the first episode are asserted to have landed, so the test cannot pass by failing to change anything. This matters because a rehearsal followed by a live demo is exactly two episodes back to back. | `tests/test_smartroom_episode_isolation.py` (`pytest -m smartroom`), using `ThreadedBrowserSession` and `SmartRoomControlClient` rather than fakes |
| Supervised Tier-4 intervention | Implemented after autonomous recovery | The Runtime first preserves transparent recovery or returns `FailureContext` to the same `System2Planner`. Only when that path cannot continue does it enter pausing/waiting/resuming states. Takeover Resume forces fresh observation, fusion, cached-grounding invalidation, and replanning; it does not create a second recovery planner. | `src/runtime/intervention.py`; `tests/test_intervention.py`, `tests/test_runtime_intervention.py`, `tests/test_precondition_repair.py` |
| Full UFO2 Windows RDP PiP | Future | No Windows child session, nested desktop host, or independent OS input/process boundary is claimed. | Requires a Windows-specific isolation provider and host UI. |
| Visual observation | VLM adapter implemented and run against an external provider | The intent episode captures PNG bytes, sends them to a configured vision client, records confidence/provenance, and fuses accepted evidence. CI still uses a fake client, but a real provider has now been configured and run: `qwen-vl-plus` over the DashScope endpoint, with the calls, the model's own answers and the resulting metrics checked in. | `src/perception/vlm_observer.py`, `scripts/run_intent_episode.py`, `tests/test_vlm_observer.py`, `tests/test_live_browser_claims.py`; `artifacts/model_value/model_value_report.json`, `artifacts/llm_demo/vision-calls.jsonl` (every call the recorded demo made) |
| Fusion calibration | Implemented, initial campaign | A labeled live campaign reports threshold ROC/confusion, false halt/miss, and detection latency. The campaign is intentionally small and should be expanded before publication. | `python -m src.pipeline --fusion-calibration` |
| Locked fusion holdout | Implemented, initial 20/10 split complete | The full 30×7 campaign can be split per condition into calibration and locked holdout; threshold is selected only on calibration and then reused unchanged on holdout. | `tests/test_fusion_holdout.py`; `artifacts/live_fusion_holdout/fusion_holdout_report.json` |
| Probabilistic/Bayesian fusion | Promotion review + gate-enabled impact complete | Bayesian can be evaluated as a report-only comparator, as shadow-mode evidence, or as a configurable `EpistemicArbiter(fusion_strategy="bayesian_gate")` gate that controls `allow_system1` while preserving existing fused-state selection. Initial/rerun holdouts favor Bayesian shadow; a 120-trial gate-enabled live ambiguous campaign reaches balanced accuracy 1.0 with no misses or false halts; a live runtime recovery-impact run shows no TSR/recovery regression. Promotion review recommends Bayesian gate as the default candidate while keeping rule-first configurable. | `tests/test_bayesian_fusion_comparator.py`, `tests/test_fusion_shadow_strategies.py`, `tests/test_bayesian_shadow_stability.py`, `tests/test_bayesian_gate_promotion_review.py`, `tests/test_gate_enabled_recovery_impact.py`, `tests/test_epistemic_runtime.py`; `artifacts/bayesian_gate_promotion_review/bayesian_gate_promotion_review.json`, `artifacts/gate_enabled_recovery_impact/gate_enabled_recovery_impact_report.json`, `artifacts/live_ambiguous_fusion_bayesian_gate_full/live_ambiguous_fusion_summary.json` |
| Noisy fusion stress set | Synthetic comparator stress only | Ambiguous/noisy source cases exercise weak stale evidence, delayed WoT recovery, low-reliability DOM, and partial missing WoT. Bayesian outperforms a fixed rule threshold on this synthetic stress set, but this is not live evidence and only motivates future live ambiguous cases. | `tests/test_noisy_fusion_stress.py`; `artifacts/noisy_fusion_stress/noisy_fusion_stress_report.json` |
| Live ambiguous fusion campaign | Initial/rerun/shadow + gate-enabled evidence complete | Ambiguous profiles use `stale_offset`, `read_delay_ms`, `drop_probability`, and `source_reliability` metadata in the smart-room environment. Two 120-trial campaigns have 20/10 per-profile locked holdouts favoring Bayesian shadow; a separate 120-trial `bayesian_gate` run confirms the configured gate can make the improved block/allow decisions live. | `tests/test_live_ambiguous_fusion_campaign.py`, `tests/test_live_ambiguous_fusion_holdout.py`; `artifacts/live_ambiguous_fusion_full/live_ambiguous_fusion_summary.json`, `artifacts/live_ambiguous_fusion_rerun/live_ambiguous_fusion_summary.json`, `artifacts/live_ambiguous_fusion_bayesian_gate_full/live_ambiguous_fusion_summary.json` |
| Open-web failure coverage | Six-family detection; five capability fixtures; recovery integration pending | Six local families retain failure detection. Excluding autocomplete, five fixtures expose generic recovery capabilities to `PlannerPort`; autonomous selection awaits the Planner owner. This is controlled local-browser interface evidence, not a real open-web claim. | `YIXIN_RUNTIME_RECOVERY_DOSSIER.md`, `tests/test_open_web_randomized_holdout.py`, `tests/test_live_browser_claims.py` |

## Verification

Run the repository checks:

```bash
uv run pytest --tb=short -q
uv run ruff check .
uv run black --check .
uv run mypy src/ --ignore-missing-imports
```

Run the real Docker + Playwright tracer bullet after starting the smart-room
environment:

```bash
uv run python -m src.pipeline --live-demo
uv run python -m src.pipeline --live-ablation
uv run python -m src.pipeline --fusion-calibration
```

The live demo writes screenshots, an episode report, transition/failure ledgers,
a recovery report, and measured metrics. The report is successful only when its
checks are derived from the persisted runtime evidence.
