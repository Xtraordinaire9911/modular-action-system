[
  {
    "skill_id": "confirm_booking",
    "description": "Confirm a room booking via the web dashboard",
    "parameters_schema": {"room": "str", "time": "str"},
    "preconditions": [{"predicate": "booking_service_available == true"}],
    "postconditions": [{"predicate": "booking_status == 'confirmed'"}],
    "allowed_backends": ["dom", "visual"],
    "preferred_backends": ["dom"],
    "rollback": null,
    "failure_modes": {},
    "timeout_ms": 5000,
    "safety_level": "low",
    "irreversible": false
  },
  {
    "skill_id": "turn_on_projector",
    "description": "Power on the room projector via WoT",
    "parameters_schema": {"room": "str"},
    "preconditions": [{"predicate": "projector_service_available == true"}],
    "postconditions": [{"predicate": "projector.power == 'on'"}],
    "allowed_backends": ["wot", "visual"],
    "preferred_backends": ["wot"],
    "rollback": {"skill_id": "turn_off_projector", "params": {}},
    "failure_modes": {},
    "timeout_ms": 3000,
    "safety_level": "low",
    "irreversible": false
  },
  {
    "skill_id": "set_temperature",
    "description": "Set thermostat target temperature via WoT",
    "parameters_schema": {"room": "str", "target": "int"},
    "preconditions": [
      {"predicate": "thermostat_service_available == true"},
      {"predicate": "target >= 16 and target <= 30"}
    ],
    "postconditions": [{"predicate": "thermostat.target_temperature == params.target"}],
    "allowed_backends": ["wot", "dom", "visual"],
    "preferred_backends": ["wot"],
    "rollback": {"skill_id": "set_temperature", "params": {"target": "__previous__"}},
    "failure_modes": {},
    "timeout_ms": 3000,
    "safety_level": "low",
    "irreversible": false
  },
  {
    "skill_id": "set_lighting",
    "description": "Set room lighting brightness via WoT",
    "parameters_schema": {"room": "str", "brightness": "int"},
    "preconditions": [{"predicate": "lighting_service_available == true"}],
    "postconditions": [{"predicate": "lighting.brightness == params.brightness"}],
    "allowed_backends": ["wot", "visual"],
    "preferred_backends": ["wot"],
    "rollback": null,
    "failure_modes": {},
    "timeout_ms": 3000,
    "safety_level": "low",
    "irreversible": false
  },
  {
    "skill_id": "verify_readiness",
    "description": "Read composite readiness status from the REST checker endpoint",
    "parameters_schema": {"room": "str"},
    "preconditions": [],
    "postconditions": [{"predicate": "readiness.ready == true"}],
    "allowed_backends": ["wot"],
    "preferred_backends": ["wot"],
    "rollback": null,
    "failure_modes": {},
    "timeout_ms": 2000,
    "safety_level": "low",
    "irreversible": false
  }
]
