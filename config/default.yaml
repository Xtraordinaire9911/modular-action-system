router:
  lambda_cost: 0.4
  lambda_reliability: 0.4
  lambda_latency: 0.2
  confidence_threshold: 0.9

executor:
  dom_timeout_ms: 5000
  wot_timeout_ms: 3000
  visual_timeout_ms: 8000
  max_retry_attempts: 2
  retry_backoff_ms: 500

vam:
  model: "Qwen2-VL"
  confidence_threshold: 0.9
  device: "cpu"

mock_env:
  web_url: "http://localhost:5000"
  wot_url: "http://localhost:5001"
